#ifndef __function_HPP
#define __function_HPP

#include "core.hpp"

namespace ac {

/*
template <typename TR>
using function0 = std::function<TR()>;

template <typename TR, typename TP1>
using function1 = std::function<TR(TP1)>;

template <typename TR, typename TP1, typename TP2>
using function2 = std::function<TR(TP1, TP2)>;

template <typename TR, typename TP1, typename TP2, typename TP3>
using function3 = std::function<TR(TP1, TP2, TP3)>;

template <typename TR, typename TP1, typename TP2, typename TP3, typename TP4>
using function4 = std::function<TR(TP1, TP2, TP3, TP4)>;

template <typename TR, typename TP1, typename TP2, typename TP3, typename TP4, typename TP5>
using function5 = std::function<TR(TP1, TP2, TP3, TP4, TP5)>;
*/

}  // namespace ac

#endif
