typedef MyObject = {
  const a :i32;
  const b :i32;
};;

const my_print = fn (a :i32, b :i32) :MyObject {

  unsafe { printf("Hello, A:%d\n", _param_a->data.i32); }
  unsafe { printf("Hello, B:%d\n", _param_b->data.i32); }

  return obj {
    a = a + 10 - b,
    b = -b + 10,
    c = obj: { const n42:i32, const n43:i32} {
      const n42 :i32 = 42,
      const n43 :i32 = 43,
    },
  }

}

export my_print: (a:i32,b:i32):MyObject = my_print;
export my_print2: (a:i32,b:i32):MyObject = my_print;
