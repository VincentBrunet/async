

const my_print = fn (a: i32, b: i32): i32 {

  const output: i32;

  unsafe {
    printf("Hello, %d\n", __a->data.i32);
    printf("Hello, %d\n", __b->data.i32);
    __output->value = __a;
  }

  return output;

}

mutable res = my_print(32, 1);

res = my_print(res, 2);

res = my_print(res, 3);
