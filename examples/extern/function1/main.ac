const dudu = import "https://google.com/didi/tutu.ac";
const dada = import "../hello";

typedef MyFunc = (a :i32, b :i32) :i32

typedef MyObject = {
  a :i32;
  b :i32;
};;

const my_print = fn (a :i32, b :i32) :MyObject {

  unsafe {
    printf("Hello, A:%d\n", __a->data.i32);
    printf("Hello, B:%d\n", __b->data.i32);
  }

  return {
    a = a + 10 - b,
    b = b + 10,
  }

}

const res1 = my_print(1, 2);
my_print(res1.b, res1.a)

const res2 = my_print(3, 4);
my_print(res2.b, res2.a)

const res3 = my_print(5, 6);
my_print(res3.b, res3.a)

return {
  mutable my_print = my_print,
  change = fn {
    res1 = res2;
  }
}
