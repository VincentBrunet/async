
const numHex = 0xff

const c1 = true
const c2 = false
const c3 = null

const lala = obj {
  const dudu = 32;
  const lol = c3
}

const dudu = expr {
  return 42 + numHex
}

mutable ttt = fn (a, b, c) {}

const hello = fn (num) {

  const f = fn (num2) {
    ttt(num, num2, c3)
  }

  f(num)
}

export const world = hello

world(32)

export mutable rofl = hello

rofl(11)