
const numHex = 0xff

const c1 = true
const c2 = false
const c3 = null

const obj = object {
  const dudu = 32
  const lol = c3
}

const hello = fn (num) {

  const f = fn (num2) {
    hello(num, num2, c3)
  }

  f(num)
}

const world = hello

world(32)


